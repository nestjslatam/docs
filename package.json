{
  "name": "@nestjslatam/docs",
  "version": "1.0.0",
  "private": true,
  "description": "Documentación oficial de NestJS Latam",
  "type": "module",
  "scripts": {
    "dev": "vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "enlaces": "npm run build && python3 scripts/comprobar-enlaces.py",
    "enlaces:todos": "npm run build && python3 scripts/comprobar-enlaces.py --externos"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
